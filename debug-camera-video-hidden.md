[OPEN] camera-video-hidden

# 问题概述

- 现象：`ClassCameraDetectPage` 启动检测后，监控画面区域仍然看不到视频画面
- 预期：启动检测并授权摄像头后，监控区域应显示实时视频，并叠加黄色检测框

# 当前已知事实

- 前端已移除中间蓝色取景框
- 页面在逻辑上会在 `cameraReady=true` 时渲染 `<video>`
- 用户反馈即使启动检测后，画面仍未显示

# 待验证假设

1. `cameraReady` 实际没有变为 `true`，导致视频节点根本未渲染。
2. 摄像头流已经拿到，但 `video.play()` 或 `loadedmetadata` 没有成功完成，视频尺寸始终为 `0`。
3. 视频节点已渲染，但被其他前景层或样式覆盖，看起来像“没有画面”。
4. 摄像头流成功接入，但浏览器自动播放策略或 `srcObject` 绑定异常导致视频元素未出帧。
5. 画面其实已存在，但当前前端状态文案不足，无法区分“未接入”“已接入但未出帧”“被遮挡”三种不同情况。

# 下一步

- 检查视频节点渲染条件与样式层级
- 添加最小前端运行时插桩
- 让用户复现并回传观测结果
